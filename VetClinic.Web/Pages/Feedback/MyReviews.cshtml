@page
@model VetClinic.Web.Pages.Feedback.MyReviewsModel
@{
    ViewData["Title"] = "My Reviews";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="text-primary">
                    <i class="fas fa-star me-2"></i>My Reviews
                </h2>
                <div class="text-muted">
                    <small>Total Reviews: @Model.TotalReviews</small>
                </div>
            </div>

            @if (Model.MyFeedback.Any())
            {
                <div class="row">
                    @foreach (var feedback in Model.MyFeedback)
                    {
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start mb-3">
                                        <div class="rating">
                                            @for (int i = 1; i <= 5; i++)
                                            {
                                                <span class="star @(i <= feedback.Rating ? "filled" : "")">
                                                    <i class="fas fa-star"></i>
                                                </span>
                                            }
                                        </div>
                                        <small class="text-muted">@feedback.CreatedDate.ToString("MMM dd, yyyy")</small>
                                    </div>

                                    <h6 class="card-title text-primary mb-2">
                                        <i class="fas fa-user-md me-1"></i>
                                        Dr. @feedback.Appointment.Doctor.FullName
                                    </h6>

                                    <p class="card-text">
                                        <small class="text-muted">
                                            <i class="fas fa-paw me-1"></i>@feedback.Appointment.Pet.Name
                                            • @feedback.Appointment.Service.Name
                                        </small>
                                    </p>

                                    @if (!string.IsNullOrEmpty(feedback.Comment))
                                    {
                                        <p class="card-text">
                                            <em>"@feedback.Comment"</em>
                                        </p>
                                    }

                                    <div class="mt-auto">
                                        <small class="text-muted">
                                            <i class="fas fa-calendar me-1"></i>
                                            Visit: @feedback.Appointment.AppointmentTime.ToString("MMM dd, yyyy")
                                        </small>
                                    </div>
                                </div>
                                <div class="card-footer bg-light">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <span class="badge bg-success">
                                            <i class="fas fa-check-circle me-1"></i>Reviewed
                                        </span>
                                        @if (feedback.Approved)
                                        {
                                            <small class="text-success">
                                                <i class="fas fa-eye me-1"></i>Public
                                            </small>
                                        }
                                        else
                                        {
                                            <small class="text-warning">
                                                <i class="fas fa-clock me-1"></i>Pending Approval
                                            </small>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>

                <!-- Pagination -->
                @if (Model.TotalPages > 1)
                {
                    <div class="row mt-4">
                        <div class="col-12">
                            <nav aria-label="My Reviews pagination">
                                <ul class="pagination justify-content-center">
                                    <!-- Previous button -->
                                    <li class="page-item @(Model.CurrentPage <= 1 ? "disabled" : "")">
                                        <a class="page-link" href="?pageNumber=@(Model.CurrentPage - 1)" aria-label="Previous">
                                            <span aria-hidden="true">&laquo;</span>
                                        </a>
                                    </li>

                                    <!-- Page numbers -->
                                    @{
                                        int startPage = Math.Max(1, Model.CurrentPage - 2);
                                        int endPage = Math.Min(Model.TotalPages, Model.CurrentPage + 2);
                                    }

                                    @for (int i = startPage; i <= endPage; i++)
                                    {
                                        <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                                            <a class="page-link" href="?pageNumber=@i">@i</a>
                                        </li>
                                    }

                                    <!-- Next button -->
                                    <li class="page-item @(Model.CurrentPage >= Model.TotalPages ? "disabled" : "")">
                                        <a class="page-link" href="?pageNumber=@(Model.CurrentPage + 1)" aria-label="Next">
                                            <span aria-hidden="true">&raquo;</span>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="text-center py-5">
                    <i class="fas fa-star fa-3x text-muted mb-3"></i>
                    <h4 class="text-muted">No Reviews Yet</h4>
                    <p class="text-muted">You haven't left any reviews for your visits yet.</p>
                    <a href="/Appointment" class="btn btn-primary">
                        <i class="fas fa-calendar-plus me-2"></i>Book an Appointment
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<style>
    .rating .star {
        color: #ddd;
        font-size: 1.2rem;
        margin-right: 2px;
    }

    .rating .star.filled {
        color: #ffc107;
    }

    .card {
        transition: transform 0.2s;
    }

    .card:hover {
        transform: translateY(-2px);
    }

    .card-footer {
        border-top: 1px solid #dee2e6;
    }
</style>
